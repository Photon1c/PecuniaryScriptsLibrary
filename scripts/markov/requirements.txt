# Core dependencies for Markov Blanket Option Pricing Model (testB.py)
# Python 3.8+ required
# Tested with Python 3.10-3.12

# Numerical computing
numpy<2.0.0,>=1.21.0  # NumPy 1.x for compatibility with other packages
pandas>=1.5.0,<3.0.0
scipy>=1.9.0,<2.0.0

# Machine Learning
scikit-learn>=1.0.0,<2.0.0
lightgbm>=3.3.0,<5.0.0
# xgboost>=1.6.0,<3.0.0  # Optional, not used in current version but available

# Visualization
matplotlib>=3.5.0,<4.0.0
seaborn>=0.12.0,<1.0.0
mplcyberpunk>=0.1.0

# Graph/Network analysis (required by testA.py)
networkx>=2.8.0,<4.0.0

# Rich terminal output
rich>=12.0.0,<14.0.0

# SHAP for model interpretability (optional but recommended)
# Note: SHAP 0.50+ requires NumPy 2.0, but we use NumPy 1.x
# If you get compatibility errors, try: pip install "shap<0.50"
# Or skip SHAP entirely - the script will work without it
# shap>=0.41.0,<0.50.0  # Uncomment if you want SHAP support with NumPy 1.x

# Note: The following are project-specific imports that should be available:
# - data_loader (from parent directory or project structure)
# - testA (MarkovBlanketAnalyzer from same directory)
